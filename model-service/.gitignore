# === Python ===
__pycache__/
*.py[cod]
*$py.class
*.pyo
*.pyd
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# === Virtual environments ===
.venv/
venv/
env/
ENV/

# === Environment / secrets ===
.env
.env.*
!.env.example

# === Jupyter ===
.ipynb_checkpoints/
*.ipynb_checkpoints
# Keep notebooks but ignore their output caches
# (outputs are committed intentionally for eval evidence)

# === Data — raw corpora can be large ===
data/raw/
# Processed and holdout are small structured files — commit them
# data/processed/ is committed
# data/holdout/ is committed

# === Model weights / large binary artefacts ===
*.bin
*.safetensors
*.gguf
*.ggml
*.pt
*.pth
*.onnx

# === Calibration scaler — commit the fitted .pkl ===
# calibration/scaler.pkl  ← intentionally NOT gitignored

# === OS junk ===
.DS_Store
Thumbs.db
desktop.ini
*.swp
*.swo
*~

# === IDE ===
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# === Pytest / coverage ===
.pytest_cache/
.coverage
htmlcov/
coverage.xml

# === Logs ===
*.log
logs/

# === Demo cache — committed intentionally for offline fallback ===
# fixtures/demo_cache.json  ← NOT gitignored

# === Ruff / mypy ===
.mypy_cache/
.ruff_cache/
