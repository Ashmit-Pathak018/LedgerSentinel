# App Engine reads requirements.txt next to app.yaml, i.e. at the repo root. Runtime deps of
# api/ only - keep in step with api/requirements.txt, minus the dev block (pytest, mypy).
fastapi==0.128.4
uvicorn[standard]==0.41.0
pydantic==2.12.4
httpx==0.28.1
python-dotenv==1.1.1
supabase==2.24.4
